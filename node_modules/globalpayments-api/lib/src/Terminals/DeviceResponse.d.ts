import { IDeviceResponse } from ".";
export declare abstract class DeviceResponse implements IDeviceResponse {
    status: string;
    command: string;
    version: string;
    deviceResponseCode: string;
    deviceResponseText: string;
    referenceNumber: string;
    constructor();
}
