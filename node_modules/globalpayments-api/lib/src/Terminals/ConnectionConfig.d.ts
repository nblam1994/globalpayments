import { Configuration, DeviceType, GatewayConfig } from "../../src";
import { IRequestIdProvider, ITerminalConfiguration } from "./Abstractions";
import { ConnectionModes, Parity } from "./Enums";
import { ConfiguredServices } from "../../src/ConfiguredServices";
export declare class ConnectionConfig extends Configuration implements ITerminalConfiguration {
    deviceType: DeviceType;
    connectionMode: ConnectionModes;
    parity: Parity;
    ipAddress: string;
    port: string;
    requestIdProvider: IRequestIdProvider;
    gatewayConfig: GatewayConfig;
    configName: string;
    connectionConfig(): void;
    configureContainer(services: ConfiguredServices): void;
    getGatewayConfig(): GatewayConfig;
    setConfigName(configName: string): void;
    getConfigName(): string;
    validate(): void;
}
