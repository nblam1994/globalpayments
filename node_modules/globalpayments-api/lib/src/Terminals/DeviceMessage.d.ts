/// <reference types="node" />
import { IDeviceMessage } from ".";
export declare class DeviceMessage implements IDeviceMessage {
    keepAlive: boolean;
    awaitResponse: boolean;
    private buffer;
    private jsonRequest;
    constructor(buffer: Buffer);
    getSendBuffer(): Buffer;
    setJsonRequest(jsonRequest: any[]): void;
    getJsonRequest(): Record<string, any>;
    toString(): string;
    getRequestField(key: string): any;
    private iterateDeep;
}
