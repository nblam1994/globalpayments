import { DeviceMessage, IDeviceCommInterface, ITerminalConfiguration } from "../../../../src";
export declare class UpaMicInterface implements IDeviceCommInterface {
    private config;
    private gatewayConfig;
    private connector;
    constructor(config: ITerminalConfiguration);
    connect(): void;
    disconnect(): void;
    send(message: DeviceMessage): any;
    parseResponse(gatewayResponse: any): any;
    private arrayCastRecursive;
}
