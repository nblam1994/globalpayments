import { IDeviceResponse } from "../../../src";
import { UpaController } from ".";
import { TerminalManageBuilder } from "../Builders";
import { DeviceInterface } from "../DeviceInterface";
export declare class UpaInterface<T extends UpaController> extends DeviceInterface<UpaController> implements DeviceInterface<UpaController> {
    private readonly upaController;
    constructor(upaController: T);
    tipAdjust(amount: number): TerminalManageBuilder;
    lineItem(leftText: string, rightText?: string): Promise<IDeviceResponse>;
}
