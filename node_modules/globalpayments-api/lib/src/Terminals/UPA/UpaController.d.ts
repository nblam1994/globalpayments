import { ConnectionConfig, IDeviceCommInterface, IDeviceInterface, ITerminalResponse } from "..";
import { TerminalAuthBuilder, TerminalManageBuilder } from "../Builders";
import { DeviceController } from "../DeviceController";
export declare class UpaController extends DeviceController {
    deviceConfig: ConnectionConfig;
    constructor(config: ConnectionConfig);
    configureInterface(): IDeviceInterface;
    manageTransaction(builder: TerminalManageBuilder): Promise<ITerminalResponse>;
    processTransaction(builder: TerminalAuthBuilder): Promise<ITerminalResponse>;
    private buildManageTransaction;
    private buildProcessTransaction;
    private isTokenRequestApplicable;
    private mapTransactionType;
    private doTransaction;
    configureConnector(): IDeviceCommInterface;
}
