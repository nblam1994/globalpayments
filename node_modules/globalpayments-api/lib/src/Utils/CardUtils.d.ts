import { Card } from "../../src/Entities/GpApi/DTO";
import { AuthorizationBuilder, PaymentMethod } from "../../src";
export declare class CardUtils {
    private static trackOnePattern;
    private static trackTwoPattern;
    private static fleetBinMap;
    private static cardTypes;
    static generateCard(builder: AuthorizationBuilder, gatewayProvider: any, maskedValues: Record<string, string>): Card;
    static getCvvIndicator(cvnPresenceIndicator: number): string;
    static parseTrackData(paymentMethod: PaymentMethod): PaymentMethod;
    static getCardType(number: string): string;
    static isFleet(cardType: string, pan: string): boolean;
}
