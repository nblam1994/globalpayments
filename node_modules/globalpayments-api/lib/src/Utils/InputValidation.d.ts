export interface IInputTypeLengths {
    city: number;
    email: number;
    firstName: number;
    lastName: number;
    phoneNumber: number;
    postalCode: number;
    province: number;
}
export type GatewayType = "portico" | "realex";
export declare function validateAmount(_gateway: GatewayType, amount: string | number): string;
export declare function validateInput(gateway: GatewayType, inputType: keyof IInputTypeLengths, input: string | undefined): string;
