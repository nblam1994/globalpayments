export declare class CountryUtils {
    static SIGNIFICANT_COUNTRY_MATCH: number;
    static SIGNIFICANT_CODE_MATCH: number;
    static getCountryInfo(country: string): any | null;
    static getCountryCodeByCountry(country: string): string | null;
    static fuzzyMatch(dict: string[], query: string, significantMatch: number): number | null;
    static fuzzyScore(term: string, query: string): number;
    static getNumericCodeByCountry(country: string): string | null;
}
