/// <reference types="node" />
import { IRequestLogger } from "+./../../src";
import { GatewayResponse } from "src/Gateways/GatewayResponse";
import { Logger } from ".";
import { IncomingHttpHeaders } from "http";
export declare class SampleRequestLogger implements IRequestLogger {
    private readonly logger;
    constructor(logger: Logger);
    requestSent(verb: string, endpoint: string, requestId: number, headers: Record<string, string | number>, data: any): void;
    responseReceived(response: GatewayResponse, requestId: number): Promise<void>;
    responseError(e: Error, requestId: number, headers: IncomingHttpHeaders): Promise<void>;
}
