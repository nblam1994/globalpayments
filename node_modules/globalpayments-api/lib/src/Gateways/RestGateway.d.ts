import { IDictionary } from "../../src";
import { Gateway } from "./Gateway";
export declare abstract class RestGateway extends Gateway {
    static AUTHORIZATION_HEADER: string;
    constructor();
    doTransaction(verb: string, endpoint: string, requestData?: string, queryStringParams?: IDictionary<string>): Promise<string>;
    maskSensitiveData(data: string): string;
    private isGpApi;
}
