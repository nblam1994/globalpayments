import { GatewayProvider } from "../../../src/Entities";
import { AcceptorConfig } from "../AcceptorConfig";
import { Configuration } from "../Configuration";
export declare abstract class GatewayConfig extends Configuration {
    acceptorConfig: AcceptorConfig;
    protected _gatewayProvider: GatewayProvider;
    dataClientId: string;
    dataClientSecret: string;
    dataClientUserId: string;
    dataClientSeviceUrl: string;
    constructor(provider: GatewayProvider);
    validate(): void;
    get gatewayProvider(): GatewayProvider;
}
