import { ConfiguredServices } from "src/ConfiguredServices";
import { Environment, IRequestLogger } from "../../src/Entities/";
export declare abstract class Configuration {
    timeout: number;
    environment: Environment;
    requestLogger: IRequestLogger;
    serviceUrl: string;
    validated: boolean;
    webProxy: unknown;
    enableLogging: boolean;
    forceGatewayTimeout: boolean;
    dynamicHeaders: Record<string, string>;
    abstract configureContainer(services: ConfiguredServices): void;
    validate(): void;
}
