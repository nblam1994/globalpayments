import { AccountType, EmvLastChipRead, EncyptedMobileType, GatewayProvider, MessageCategory, SdkUiType, StoredCredentialInitiator } from "../../src";
export declare class EnumMapping {
    static mapAccountType(gateway: GatewayProvider, accountType: AccountType): string | null;
    static mapDigitalWalletType(gateway: GatewayProvider, type: EncyptedMobileType): string | null;
    static mapStoredCredentialInitiator(gateway: GatewayProvider, value: StoredCredentialInitiator): string;
    static mapEmvLastChipRead(gateway: GatewayProvider, value: EmvLastChipRead): string | null;
    static mapCardType(gateway: GatewayProvider, value: string): string;
    static mapSdkUiType(gateway: GatewayProvider, value: SdkUiType[]): string[] | SdkUiType[];
    static mapMessageCategory(gateway: GatewayProvider, value: MessageCategory): string | MessageCategory;
}
