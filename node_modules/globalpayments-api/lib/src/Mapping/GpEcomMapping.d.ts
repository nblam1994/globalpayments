import { Element } from "@azz/elementtree";
import { BaseBuilder, ManagementBuilder } from "../../src";
import { Transaction } from "../../src/Entities";
export declare class GpEcomMapping {
    static mapAuthRequestType(builder: BaseBuilder<Transaction>): string;
    static mapResponse(rawResponse: string, acceptedCodes: string[]): Transaction;
    protected static checkResponse(root: Element, acceptedCodes?: string[]): void;
    static mapManageRequestType(builder: ManagementBuilder): "settle" | "hold" | "rebate" | "release" | "void" | "unknown";
}
