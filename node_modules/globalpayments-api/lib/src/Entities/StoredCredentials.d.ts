import { StoredCredentialInitiator, StoredCredentialReason, StoredCredentialSequence, StoredCredentialType } from ".";
export declare class StoredCredential {
    type: StoredCredentialType;
    initiator: StoredCredentialInitiator;
    sequence: StoredCredentialSequence;
    schemeId: string;
    reason: StoredCredentialReason;
    cardBrandTransactionId: string;
}
