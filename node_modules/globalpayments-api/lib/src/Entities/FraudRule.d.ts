import { FraudFilterMode } from ".";
export declare class FraudRule {
    key: string;
    mode: FraudFilterMode;
    description: string;
    result: string;
    constructor(key: string, mode: FraudFilterMode, description: string, result: string);
}
