import { ConnectionConfig, IDeviceInterface } from "../../src";
export declare class DeviceService {
    static create(config: ConnectionConfig, configName?: string): IDeviceInterface;
    static findDeviceController(configName?: string): IDeviceInterface;
}
