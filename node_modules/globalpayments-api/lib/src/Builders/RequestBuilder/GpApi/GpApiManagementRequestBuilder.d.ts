import { BaseBuilder, DccRateData, GpApiRequest, IRequestBuilder, Transaction } from "../../../../src";
export declare class GpApiManagementRequestBuilder implements IRequestBuilder {
    private maskedValues;
    canProcess(builder: BaseBuilder<Transaction>): boolean;
    buildRequest(builder: BaseBuilder<Transaction>): GpApiRequest;
    getDccRate(dccRateData: DccRateData): {
        id: string;
    };
    buildRequestFromJson(jsonRequest: string): void;
}
