import { Element as element } from "@azz/elementtree";
import { Customer, GpEcomConfig } from "../../../../src";
export declare abstract class GpEcomRequestBuilder {
    buildSupplementaryData(supplementaryData: Record<string, string | string[]>, request: element): void;
    protected buildEnvelope(transaction: element): string;
    protected numberFormat(amount: number | string): string;
    protected generateHash(config: GpEcomConfig, timestamp: string, orderId: string, amount: string, currency: string, paymentData: string, verify?: boolean): string;
    protected buildCustomer(customer: Customer): element;
}
